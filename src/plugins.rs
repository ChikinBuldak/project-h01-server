pub mod webrtc {
    use bevy::prelude::*;

    /// Plugin for WebRTC inside bevy's run
    pub struct WebRTCPlugin;

    impl Plugin for WebRTCPlugin {
        fn build(&self, app: &mut App) {
            
        }
    }
}